{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. remote.urls must cover whatever origin tauri.conf.json's window actually loads, or the webview gets zero Tauri permissions (including store:default — the offline queue's Tauri Store adapter would silently stop working). scripts/check-production-config.mjs verifies this stays true before every production build. Do not remove the localhost entry, it's still needed for local dev.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://localhost:3001/*", "https://munshiman.com/*"]
  },
  "permissions": [
    "core:default",
    "opener:default",
    "store:default",
    "updater:default",
    "notification:default"
  ]
}
